'use strict';

var React = require('react');

var Navigation = React.createClass({
  render: function () {
    return (
      <div className='nav'>
        NAVIGATION
      </div>
    );
  }
});

module.exports = Navigation;
